<view class="container" >
    <view class="items" >
        <lesson-item wx:for="{{all_lesson_cases}}" class="row" entity = "{{item}}" bind:tap="clicklesson">
        </lesson-item>
    </view>
    
</view>
