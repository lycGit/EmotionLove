<view class="container" >
    <view class="items" >
        <skill-item wx:for="{{all_skill_cases}}" class="row" entity = "{{item}}" bind:tap="showWebView">
        </skill-item>
    </view>

    
</view>
